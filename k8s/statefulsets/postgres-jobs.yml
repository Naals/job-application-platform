apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-jobs
  namespace: jobplatform
spec:
  serviceName: postgres-jobs-service
  replicas: 1
  selector:
    matchLabels:
      app: postgres-jobs
  template:
    metadata:
      labels:
        app: postgres-jobs
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: jobs_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: jobs-db-username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: jobs-db-password
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          volumeMounts:
            - name: postgres-jobs-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "jobs_user", "-d", "jobs_db"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "jobs_user", "-d", "jobs_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: postgres-jobs-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-jobs-service
  namespace: jobplatform
spec:
  clusterIP: None
  selector:
    app: postgres-jobs
  ports:
    - port: 5432
      targetPort: 5432